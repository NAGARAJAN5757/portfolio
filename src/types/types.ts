export type EduDetail = { course: string; courseDetail: string }
export type MenuItem = { id: string; item: string }
export type Project = {
    title: string
    image: string
    description: string
    technologies: string[]
    link: string
}
export type Skill = string
